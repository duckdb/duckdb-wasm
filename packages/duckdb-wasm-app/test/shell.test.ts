export function testShell(): void {
    beforeAll(async () => {});

    beforeEach(async () => {});

    describe('Parser', () => {
        it('syntax error', async () => {
            expect(1).toEqual(1);
        });
    });
}
