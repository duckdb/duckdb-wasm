export function testShell() {
    beforeAll(async () => {});

    beforeEach(async () => {});

    describe('Parser', () => {
        it('syntax error', async () => {
            expect(1).toEqual(1);
        });
    });
}
