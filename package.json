{
    "private": true,
    "workspaces": [
        "packages/*"
    ],
    "scripts": {
        "format": "prettier --write packages"
    },
    "dependencies": {
        "prettier": "^2.3.0"
    }
}
