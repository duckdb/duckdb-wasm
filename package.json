{
    "repository": {
        "type": "git",
        "url": "https://github.com/kimmolinna/duckdb-wasm.git"
    },
    "private": true,
    "workspaces": [
        "packages/*",
        "examples/*"
    ],
    "scripts": {
        "lint:fix": "yarn workspace @kimmolinna/duckdb-wasm lint --fix && yarn workspace @kimmolinna/benchmarks lint --fix && yarn workspace @kimmolinna/duckdb-wasm-shell lint --fix",
        "docs": "yarn workspace @kimmolinna/duckdb-wasm docs",
        "benchmarks:build:browser": "yarn workspace @kimmolinna/benchmarks build:browser",
        "benchmarks:build:node": "yarn workspace @kimmolinna/benchmarks build:node",
        "benchmarks:build": "yarn workspace @kimmolinna/benchmarks build"
    },
    "devDependencies": {
        "badge-maker": "^3.3.1"
    }
}
